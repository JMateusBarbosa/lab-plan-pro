name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.23

      - name: Regenerate lockfile (temporary diagnostic)
        run: |
          bun install --lockfile-only
          echo "LOCKFILE_BASE64_BEGIN"
          base64 -w0 bun.lock
          echo
          echo "LOCKFILE_BASE64_END"
          exit 1

      - name: Build
        run: bun run build

      - name: Verify generated route tree
        run: git diff --exit-code -- src/routeTree.gen.ts

      - name: Lint
        run: >-
          bunx eslint . --rule 'prettier/prettier: off'

  database-security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.117.0

      - name: Start local Supabase
        run: supabase start

      - name: Run database security tests
        run: supabase test db

      - name: Stop local Supabase
        if: always()
        run: supabase stop --no-backup

  edge-functions:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Test Edge Function CORS policy
        run: >-
          deno test --allow-env=ALLOWED_ORIGINS
          supabase/functions/_shared/cors_test.ts

      - name: Type-check Edge Functions
        run: >-
          deno check --node-modules-dir=none
          supabase/functions/provision-laboratory/index.ts
          supabase/functions/update-laboratory/index.ts
          supabase/functions/set-laboratory-status/index.ts
          supabase/functions/manage-laboratory-credentials/index.ts
